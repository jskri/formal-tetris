//! spec: instance.rs — concrete instantiation (§11, `implementation.md`).
//!
//! §11/§6.0: no `Params` trait of its own, so nothing for this file to
//! `impl`, unlike every prior layer's `instance.rs`.

pub use t5::instance::*;
