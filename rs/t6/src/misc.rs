//! spec: main.rs — the `Action` shape for `T6.v` (§15.1, `implementation.md`).
//!
//! §15.1: req-piece-kick extends rotation's behavior, not a new `Action`
//! variant or keybinding.

pub use t5::misc::*;
